{
    "name": "deploy",
    "dependencies": {
        "@sap/hdi-deploy": "^4.0"
    },
    "engines": {
        "node": "^10.0.0 || ^12.0.0 || ^14.0.0"
    },
    "scripts": {
        "start": "node node_modules/@sap/hdi-deploy/deploy.js  --auto-undeploy --exit"
    }
}
